{# Static no-CSS / no-JS hint block.

   No inputs. Shown only at depth 0. The no-CSS hint is visible by default;
   the no-JS hint is hidden inline and later revealed by CSS or hidden by JS.
#}
<div class="anndata-repr__hint-nocss">
<em>Styled representation available in Jupyter and trusted notebooks (colors, search, type highlighting).</em>
</div>
<div class="anndata-repr__hint-nojs" style="display:none">
<em>Interactive features (search, copy, category wrapping) require JavaScript. Trust this notebook to enable them.</em>
</div>
